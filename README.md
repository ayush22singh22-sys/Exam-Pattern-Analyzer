# 📄 Exam Pattern Analyzer

A Python tool that extracts text from PDF files, cleans it using regex, and saves the output as structured JSON.

## Project Structure

```
Exam Pattern Analyzer/
├── pdfs/            # Place your PDF files here
├── output/          # Extracted & cleaned text saved here (as JSON)
├── src/             # All Python source code
│   └── __init__.py
├── requirements.txt # Python dependencies
└── README.md        # You are here!
```

## Setup

```bash
# 1. Create a virtual environment
python -m venv venv

# 2. Activate it
# Windows:
venv\Scripts\activate
# Mac/Linux:
source venv/bin/activate

# 3. Install dependencies
pip install -r requirements.txt
```

## Usage

_(Coming soon — we'll build this step by step!)_
